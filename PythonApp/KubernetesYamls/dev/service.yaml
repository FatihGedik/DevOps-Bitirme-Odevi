apiVersion: v1
kind: Service
metadata:
  name: tbbpython
  namespace: dev
spec:
  selector:
    app: tbbpython
  type: NodePort
  ports:
  - name: http
    port: 5001
    targetPort: 5001
    protocol: TCP
